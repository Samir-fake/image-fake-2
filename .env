# Image Extractor - Environment Variables Template
# Copy this file to .env and fill in your values

# Required for production
NODE_ENV=production
PORT=3000

# Session configuration (REQUIRED - generate a random string)
SESSION_SECRET=your-super-secret-random-string-change-this

# Optional: PostgreSQL Database for persistent storage
# If not provided, the app will use in-memory storage
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Optional: Custom configuration
# MAX_CONCURRENT_EXTRACTIONS=3
# TEMP_CLEANUP_INTERVAL=300000

# Puppeteer configuration (for shared hosting environments)
# PUPPETEER_ARGS=--no-sandbox,--disable-setuid-sandbox,--disable-dev-shm-usage